const calculateProgressService = {
    calculateProgressByKPIObjective: async function () {
        try {

        } catch (error) {

        }
    },

    /////////cuándo se calcula -> cuando se añade una tarea - cuando se actualiza su estado - cuando se elimina la tarea

    //cómo se calcula
    //por qué se calcula
    calculateProgressByTasksObjective: async function () {
        try {

        } catch (error) {

        }
    },
    calculateProgressByBinaryObjective /*lo que sería: cumplido o no cumplido, no hay mucha vuelta*/: async function () {
        try {
            
        } catch (error) {
            
        }

    },
    calculateProgressAutomaticIndexModule: async function(){
        try {
            
        } catch (error) {
            
        }
    }
}

module.exports = calculateProgressService