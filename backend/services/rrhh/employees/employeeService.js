const Employee = require('../../../models/rrhh/employees/employeeModel')

const employeeService = {
    createEmployee: async function (data, enterpriseId){
        try {
                  
        } catch (error) {
            
        }
    },
}

module.exports = employeeService